# Real DB credentials — never commit these. See the .example files for the
# expected shape; copy one and fill in real values locally / on the server.
backend/hscr-db.php
hscr-db.local.php

.DS_Store
